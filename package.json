{
  "name": "rythme",
  "version": "1.0.0",
  "description": "Rythme - A complete solution to learn Rythm!",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently  --names \"SERVER,CLIENT\" -c \"blue,magenta\" \"npm run server\" \"npm run client\"",
    "client": "npm start --prefix client",
    "server": "npm start --prefix api",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "WRC Driverco",
  "license": "ISC",
  "dependencies": {
  }
}
